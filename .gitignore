# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Node (only if tooling is added later)
node_modules/
npm-debug.log*

# Local scratch
tmp/
*.local

# Files delivered into the folder by the Claude desktop app, and stray
# scratch images — neither belongs in the published site.
Claude outputs/
*.bmp

# The workflow copy kept at the repo root for reference; the live one is
# .github/workflows/deploy.yml
deploy-workflow.yml

# Generated by demo/run-demo.mjs; regenerate it rather than committing a
# snapshot that will silently go stale.
demo/report.html

# Vercel: the account link and the generated public artifact. `dist` is
# disposable output, never source — the build clears and rewrites it.
/.vercel/
/dist/

# Downloaded model weights and local browser measurements
*.gguf
/test-results/
/kor.traineddata
/output/
